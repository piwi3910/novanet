apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: novanet
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: >-
      agent=ghcr.io/azrtydxb/novanet/novanet-agent,
      dataplane=ghcr.io/azrtydxb/novanet/novanet-dataplane
    argocd-image-updater.argoproj.io/agent.update-strategy: semver
    argocd-image-updater.argoproj.io/dataplane.update-strategy: semver
    argocd-image-updater.argoproj.io/agent.helm.image-name: image.agent.repository
    argocd-image-updater.argoproj.io/agent.helm.image-tag: image.agent.tag
    argocd-image-updater.argoproj.io/dataplane.helm.image-name: image.dataplane.repository
    argocd-image-updater.argoproj.io/dataplane.helm.image-tag: image.dataplane.tag
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/azrtydxb/novanet
    path: deploy/helm/novanet
    targetRevision: main
    helm:
      releaseName: novanet
      valuesObject:
        image:
          agent:
            repository: ghcr.io/azrtydxb/novanet/novanet-agent
            tag: v1.11.0
          dataplane:
            repository: ghcr.io/azrtydxb/novanet/novanet-dataplane
            tag: v1.11.0
        config:
          routingMode: "native"
        routing:
          protocol: "bgp"
          bfd:
            enabled: true
            minRxMs: 300
            minTxMs: 300
            detectMultiplier: 3
          peers:
            - neighbor_address: "192.168.100.2"
              remote_as: 65000
              description: "TOR-1"
              bfd_enabled: true
            - neighbor_address: "192.168.100.3"
              remote_as: 65000
              description: "TOR-2"
              bfd_enabled: true
        l4lb:
          enabled: true
  destination:
    server: https://kubernetes.default.svc
    namespace: novanet-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
